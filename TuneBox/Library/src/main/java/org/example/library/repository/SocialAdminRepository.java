package org.example.library.repository;

import org.example.library.model.SocialAdmin;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SocialAdminRepository extends JpaRepository<SocialAdmin, Long> {
}
